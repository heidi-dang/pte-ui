import { StudentPageContainer } from '../StudentPageContainer';
import { RoutePlaceholder } from './RoutePlaceholder';

export function AchievementsPage() {
  return <RoutePlaceholder title="Achievements" route="/student/achievements" />;
}
