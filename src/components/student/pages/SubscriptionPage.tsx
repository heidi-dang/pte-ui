import { StudentPageContainer } from '../StudentPageContainer';
import { RoutePlaceholder } from './RoutePlaceholder';

export function SubscriptionPage() {
  return <RoutePlaceholder title="Subscription" route="/student/subscription" />;
}
