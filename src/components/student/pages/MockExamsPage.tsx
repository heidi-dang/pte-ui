import { StudentPageContainer } from '../StudentPageContainer';
import { RoutePlaceholder } from './RoutePlaceholder';

export function MockExamsPage() {
  return <RoutePlaceholder title="Mock Exams" route="/student/mock-exams" />;
}
